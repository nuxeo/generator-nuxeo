#!/bin/sh -
GENERATOR_IMG=nuxeo/generator:latest

which docker &> /dev/null || ( echo "Docker is missing from path." && exit 1; )
docker run -ti --rm -v "`pwd`:/workdir" -e "workdir=`basename $PWD`" ${GENERATOR_IMG} $@
